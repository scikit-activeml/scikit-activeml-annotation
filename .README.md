## Setup
- Recommended: Setup and active virtual environment (venv)
$ python -m venv .venv
$ ./.venv/Scripts/activate

- Install dependencies
$ pip install -r .requirements.md

## Start the App
Make sure the venv is enabled, if used in setup and not allready done so.
$ ./.venv/Scripts/activate
$ cd src
$ python app.py


