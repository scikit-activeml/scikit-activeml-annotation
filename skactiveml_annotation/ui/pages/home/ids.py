from skactiveml_annotation.util.utils import make_ids

ids = make_ids(__name__)

# --- Triggers ---
URL = ids('url')
URL_INIT = ids('url-init')
NEXT_PAGE_TRIGGER = ids('next-page-trigger')

RADIO_SELECTION = ids('radio-selection')
STEPPER = ids('stepper')
UI_CONTAINER = ids('ui-container')
SELECTION_PROGRESS = ids('selection-progress')

# --- Buttons ---
CONFIRM_BUTTON = ids('confirm_button')
BACK_BUTTON = ids('back_button')

KEYBOARD = ids('keyboard')

